{
  "name": "@bp/backend",
  "version": "1.0.0",
  "description": "simple backend",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "type": "module",
  "scripts": {
    "dev": "tsc-watch --onSuccess \"cross-env DEBUG=express:router node ./dist/server.js\"",
    "build": "tsc --build"
  },
  "dependencies": {
    "@bp/shared": "^1.0.0",
    "@fastify/cors": "^8.2.0",
    "fastify": "^4.12.0",
    "fastify-type-provider-zod": "^1.1.9",
    "nanoid": "^4.0.1",
    "zod": "^3.20.3"
  },
  "devDependencies": {
    "@types/node": "^18.13.0"
  }
}